#include <iostream>

/*
 * Pay close attention to formatting.
 * Create a very basic program, compile and run it.
 * Add another line, compile and run again. If possible, think of 
 * a way to check that your program so far really does what you think
 * it does.
 * Carry on adding a line, building and testing, add another line,
 * and so on.
 */
int main() {
    std::cout << "Hello, world!" << std::endl;
    return 0;
}
